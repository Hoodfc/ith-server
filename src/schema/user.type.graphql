type User {
    id: ID!
    name: String!
    password: String
    email: String!
}

type Query {
    hello: String!
    getUser(name: String!): User
    getAllUsers: [User!]!    
}

type Mutation {
    createUser(name: String!, email: String, password: String!): User
}